import {
  fail,
  assert,
  assertEquals,
  assertMatch,
  assertThrows,
} from 'https://deno.land/std@0.86.0/testing/asserts.ts';

export {
  fail,
  assert,
  assertEquals,
  assertMatch,
  assertThrows,
};
