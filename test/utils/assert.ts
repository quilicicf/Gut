import {
  fail,
  assertEquals,
  assertMatch,
  assertThrows,
  assertObjectMatch,
} from 'https://deno.land/std/testing/asserts.ts';

export {
  fail,
  assertEquals,
  assertMatch,
  assertThrows,
  assertObjectMatch,
};
