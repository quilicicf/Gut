extends: '@talend/eslint-config/api-test.yaml'
env:
  jest/globals: true
plugins:
  - jest
rules:
  import/no-unresolved:
    - error
    - ignore:
        - ^http
  import/prefer-default-export:
    - off
